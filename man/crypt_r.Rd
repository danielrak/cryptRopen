% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crypt_r.R
\name{crypt_r}
\alias{crypt_r}
\title{Industrialized dataset variables encryption.}
\usage{
crypt_r(
  mask_folder_path,
  mask_file,
  output_path,
  intermediate_path,
  encryption_key,
  algorithm = "md5",
  correspondence_table = TRUE,
  engine = c("auto", "in_memory", "streaming"),
  chunk_size = 1000000L
)
}
\arguments{
\item{mask_folder_path}{Folder path of the excel mask.}

\item{mask_file}{File name (with extension) of the excel mask.}

\item{output_path}{Folder path where the encrypted datasets
will be created.}

\item{intermediate_path}{Folder path where intermediate files
(correspondence tables for e.g) will be created.}

\item{encryption_key}{Character vector.}

\item{algorithm}{Algorithm to use.
From digest::digest()'s algo argument.}

\item{correspondence_table}{Logical vector. If TRUE, the correspondence tables will computed
and stored in the indicated intermediate_path.}

\item{engine}{One of \code{"auto"}, \code{"in_memory"}, \code{"streaming"}.
Selects the per-row processing engine. \code{"auto"} (default) picks
\code{"streaming"} for \code{.parquet} / \code{.csv} inputs and \code{"in_memory"}
for every other format. \code{"in_memory"} reads the full input into
RAM (historical behaviour). \code{"streaming"} scans the input by
chunks via arrow and writes progressively. Introduced in Phase
1.D.4.a; the streaming engine itself lands in 1.D.4.b/c.}

\item{chunk_size}{Integer. Number of rows per chunk when \code{engine}
resolves to \code{"streaming"}. Ignored by \code{"in_memory"}. Default
\code{1e6}.}
}
\description{
This function consolidates a set of procedures
made to encrypt variables from datasets.
Variables to encrypt and input datasets can be in any number,
following user's needs.
The design tries to approach parallel processing.
}
