% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cryptR_results.R
\name{cryptR_results}
\alias{cryptR_results}
\title{Per-output disk-oriented view of an async \code{crypt_r()} job}
\usage{
cryptR_results(job)
}
\arguments{
\item{job}{A \code{cryptR_job} object, as returned by \code{\link[=crypt_r]{crypt_r()}}.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{encrypted_file} (character): row identifier (dedup'd file name).
\item \code{output_file_path} (character): full path where the output is /
would be written.
\item \code{exists} (logical): result of \code{file.exists(output_file_path)} at
call time.
\item \code{size_bytes} (numeric): size recorded by the worker post-write,
\code{NA} when the task has not produced a payload yet.
\item \code{sha256} (character): sha256 of the output at worker-completion
time, \code{NA} otherwise.
\item \code{success} (logical): whether the engine reported a clean end.
\item \code{error_message} (character): concatenated engine errors, \code{NA} on
success.
}
}
\description{
Returns one row per filtered mask row with (a) the expected output
file path, (b) whether it currently exists on disk, and (c) the
size/hash the worker captured right after writing it. Introduced in
Phase 2.B as a companion to \code{\link[=cryptR_status]{cryptR_status()}} — the two views are
orthogonal and join cleanly on \code{encrypted_file}.
}
\details{
Running tasks contribute a row with \code{exists = FALSE} and NA for
\code{size_bytes} / \code{sha256}. Tasks that failed before reaching the final
export contribute a row with \code{success = FALSE}, \code{exists} reflecting
the live file state (which may still be \code{FALSE} because the engine
short-circuited), and \code{error_message} set.

The \code{size_bytes} / \code{sha256} values are the ones the engine recorded
inside the worker. If the output file has been modified or removed
between then and the call to \code{cryptR_results()}, those columns do
\strong{not} reflect that — they describe what was produced, not what is
currently on disk. Use \code{file.info()} / \code{digest::digest()} for live
measurements when needed.
}
\examples{
\dontrun{
job <- crypt_r(...)
cryptR_wait(job)
cryptR_results(job)
}
}
\seealso{
\code{\link[=cryptR_status]{cryptR_status()}}, \code{\link[=summary.cryptR_job]{summary.cryptR_job()}}.
}
