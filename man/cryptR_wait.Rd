% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cryptR_job.R
\name{cryptR_wait}
\alias{cryptR_wait}
\title{Block until an asynchronous \code{crypt_r()} job finishes}
\usage{
cryptR_wait(job, timeout = Inf, poll_interval = 0.1)
}
\arguments{
\item{job}{A \code{cryptR_job} object.}

\item{timeout}{Numeric. Maximum wait in seconds. Defaults to \code{Inf}.
On expiration, an error of class \code{cryptR_timeout} is raised.}

\item{poll_interval}{Numeric. Polling period in seconds. Defaults to 0.1.}
}
\value{
\code{invisible(job)}.
}
\description{
Polls every task in \code{job} and returns once they are all resolved
(either \code{done} or \code{failed}). A failed task does \emph{not} raise an
exception here — its state is simply visible via \code{\link[=cryptR_status]{cryptR_status()}}.
}
\examples{
\dontrun{
job <- crypt_r(...)
cryptR_wait(job, timeout = 60)
}
}
\seealso{
\code{\link[=cryptR_status]{cryptR_status()}}, \code{\link[=cryptR_collect]{cryptR_collect()}}.
}
