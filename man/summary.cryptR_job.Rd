% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cryptR_job.R
\name{summary.cryptR_job}
\alias{summary.cryptR_job}
\title{Dashboard summary of an asynchronous \code{crypt_r()} job}
\usage{
\method{summary}{cryptR_job}(object, ...)
}
\arguments{
\item{object}{A \code{cryptR_job} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{summary.cryptR_job} — a named list
with elements \code{n_tasks}, \code{counts}, \code{elapsed_sec}, \code{n_workers},
\code{total_rows}, \code{output_path}, \code{log_written}, \code{status} (the full
\code{\link[=cryptR_status]{cryptR_status()}} dataframe).
}
\description{
Companion to \code{print()}. Returns a structured list (class
\code{summary.cryptR_job}) carrying the aggregates a monitoring script
typically needs: total task count, per-state counts, elapsed
seconds, active workers, total rows processed (sum over resolved
tasks — excludes NA), output path, and the full per-task status
dataframe.
}
\details{
The object has its own \code{print()} method that renders a compact
dashboard.
}
\examples{
\donttest{
# Build a tiny job (see ?crypt_r for a more detailed walkthrough)
work_dir <- file.path(tempdir(), "cryptR_summary_example")
mask_dir <- file.path(work_dir, "mask")
out_dir  <- file.path(work_dir, "output")
int_dir  <- file.path(work_dir, "intermediate")
for (d in c(mask_dir, out_dir, int_dir)) {
  dir.create(d, recursive = TRUE, showWarnings = FALSE)
}

input_file <- system.file("extdata", "persons.csv", package = "cryptRopen")
mask <- data.frame(
  folder_path     = dirname(input_file),
  file            = basename(input_file),
  encrypted_file  = "persons_crypt.csv",
  vars_to_encrypt = "email",
  vars_to_remove  = NA,
  to_encrypt      = "X",
  stringsAsFactors = FALSE
)
writexl::write_xlsx(mask, file.path(mask_dir, "mask.xlsx"))

job <- crypt_r(
  mask_folder_path  = mask_dir,
  mask_file         = "mask.xlsx",
  output_path       = out_dir,
  intermediate_path = int_dir,
  encryption_key    = "demo-key",
  n_workers         = 1L
)
job <- cryptR_collect(job)
summary(job)

unlink(work_dir, recursive = TRUE)
}
}
\seealso{
\code{\link[=cryptR_status]{cryptR_status()}}, \code{\link[=cryptR_collect]{cryptR_collect()}}.

Other async_job: 
\code{\link{cryptR_collect}()},
\code{\link{cryptR_results}()},
\code{\link{cryptR_status}()},
\code{\link{cryptR_wait}()},
\code{\link{crypt_r}()}
}
\concept{async_job}
