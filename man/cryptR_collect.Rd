% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cryptR_job.R
\name{cryptR_collect}
\alias{cryptR_collect}
\title{Finalize an asynchronous \code{crypt_r()} job}
\usage{
cryptR_collect(job, timeout = Inf, poll_interval = 0.1)
}
\arguments{
\item{job}{A \code{cryptR_job} object.}

\item{timeout}{Numeric. Maximum wait in seconds. Defaults to \code{Inf}.
On expiration, an error of class \code{cryptR_timeout} is raised.}

\item{poll_interval}{Numeric. Polling period in seconds. Defaults to 0.1.}
}
\value{
\code{invisible(job)} — with \code{log_written = TRUE} and, when
applicable, \code{daemons_torn_down = TRUE}. The modifications are
applied to the returned object only; S3 objects are not mutable
in place in R, so callers who want the updated flags must capture
the return value (\code{job <- cryptR_collect(job)}).
}
\description{
Waits for all tasks to resolve (see \code{\link[=cryptR_wait]{cryptR_wait()}}), extracts the
per-row results, re-publishes the correspondence tables into the
parent's \code{.cryptRopen_env} (so \code{get_correspondence_tables()} sees them
after an async run), writes the recap log
\verb{log_crypt_r_<timestamp>.xlsx} under \code{job$output_path}, and — when
\code{crypt_r()} created the mirai daemons itself
(\code{daemons_owned_by_job = TRUE}) — tears them down.
}
\details{
Idempotent on two independent axes:
\itemize{
\item \code{job$log_written} guards the log-writing + TC re-injection block,
so a manual \code{cryptR_collect()} called \emph{after} the auto watcher
has already run is a no-op — no duplicate xlsx, no TCs stored
twice.
\item \code{job$daemons_torn_down} guards the \code{mirai::daemons(0)} call so
a second collect does not attempt a double teardown.
}

When daemons were set up externally before \code{crypt_r()} was called,
\code{daemons_owned_by_job} is \code{FALSE} and teardown is never attempted —
the user retains control of their own daemons.
}
\examples{
\donttest{
# Build a tiny job (see ?crypt_r for a more detailed walkthrough)
work_dir <- file.path(tempdir(), "cryptR_collect_example")
mask_dir <- file.path(work_dir, "mask")
out_dir  <- file.path(work_dir, "output")
int_dir  <- file.path(work_dir, "intermediate")
for (d in c(mask_dir, out_dir, int_dir)) {
  dir.create(d, recursive = TRUE, showWarnings = FALSE)
}

input_file <- system.file("extdata", "persons.csv", package = "cryptRopen")
mask <- data.frame(
  folder_path     = dirname(input_file),
  file            = basename(input_file),
  encrypted_file  = "persons_crypt.csv",
  vars_to_encrypt = "email",
  vars_to_remove  = NA,
  to_encrypt      = "X",
  stringsAsFactors = FALSE
)
writexl::write_xlsx(mask, file.path(mask_dir, "mask.xlsx"))

job <- crypt_r(
  mask_folder_path  = mask_dir,
  mask_file         = "mask.xlsx",
  output_path       = out_dir,
  intermediate_path = int_dir,
  encryption_key    = "demo-key",
  n_workers         = 1L
)
job <- cryptR_collect(job)

cryptR_status(job)
unlink(work_dir, recursive = TRUE)
}
}
\seealso{
\code{\link[=cryptR_status]{cryptR_status()}}, \code{\link[=cryptR_wait]{cryptR_wait()}}.

Other async_job: 
\code{\link{cryptR_results}()},
\code{\link{cryptR_status}()},
\code{\link{cryptR_wait}()},
\code{\link{crypt_r}()},
\code{\link{summary.cryptR_job}()}
}
\concept{async_job}
